server:
  port: 9000

spring:
  application:
    name: prod

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # mysql 8버전
    # driver-class-name: com.mysql.jdbc.Driver # mysql 5버전
    url: jdbc:mysql://whereismyteamdb.cjr7wntaenbp.ap-northeast-2.rds.amazonaws.com:3306/whereismyteamDB?autoReconnect=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username:  admin
    password:  '#team2022'

  jpa:
    show-sql: true
    generate-ddl: true
    #database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate.enable_lazy_load_no_trans: true
      hibernate.format_sql: true


